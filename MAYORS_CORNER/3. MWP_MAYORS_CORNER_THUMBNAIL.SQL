INSERT
INTO MWP_MAYORS_CORNER_THUMBNAIL
(
  MC_THUMBNAIL_CONTENT_ID,
  TEMP_PATH
)
SELECT mwp.MC_ID,
      port.PIC_LARGE
FROM MWP_MAYORS_CORNER mwp
LEFT JOIN PORTAL.TBL_BLOG_PIC port ON mwp.TEMP_ID = port.BLOG_ID;
